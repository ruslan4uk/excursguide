<IfModule mod_rewrite.c>
RewriteEngine on
RewriteCond %{REQUEST_URI} !^public
RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

# RewriteRule ([^.]+\.(jpe?g|gif|bmp|png))$ http://localhost:8000/$1 [R=301,L,NC]
